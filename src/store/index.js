import Vue from 'vue'
import Vuex from 'vuex'
import formSubmit from './modules/formSubmit'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    formSubmit
  }
})
